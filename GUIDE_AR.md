# دليل المشروع الجديد - البنية المنفصلة

## ✅ ما تم إنجازه

تم بنجاح فصل المشروع إلى قسمين رئيسيين:

### 1️⃣ Backend (الواجهة الخلفية)

📁 **المجلد**: `backend/`

**الملفات المنشأة:**

- ✅ `backend/config.php` - إعدادات قاعدة البيانات + دوال مساعدة
- ✅ `backend/classes/Product.php` - كلاس إدارة المنتجات
- ✅ `backend/classes/Invoice.php` - كلاس إدارة الفواتير
- ✅ `backend/classes/Settings.php` - كلاس إدارة الإعدادات
- ✅ `backend/classes/Reports.php` - كلاس التقارير والإحصائيات

**نقاط النهاية API:**

- ✅ `backend/api/products.php` - واجهة المنتجات
- ✅ `backend/api/invoices.php` - واجهة الفواتير
- ✅ `backend/api/settings.php` - واجهة الإعدادات
- ✅ `backend/api/reports.php` - واجهة التقارير

### 2️⃣ Frontend (الواجهة الأمامية)

📁 **المجلد**: `frontend/`

**الملفات المنشأة:**

- ✅ `frontend/assets/css/main.css` - ملف الأنماط الرئيسي
- ✅ `frontend/assets/js/api.js` - وحدة التواصل مع API
- ✅ `frontend/assets/js/main.js` - الدوال المشتركة
- ✅ `frontend/pages/index.html` - لوحة التحكم

**صفحات HTML المطلوبة (يجب إنشاؤها):**

- ⏳ `frontend/pages/products.html` - صفحة المنتجات
- ⏳ `frontend/pages/invoices.html` - قائمة الفواتير
- ⏳ `frontend/pages/create_invoice.html` - إنشاء فاتورة
- ⏳ `frontend/pages/edit_invoice.html` - تعديل فاتورة
- ⏳ `frontend/pages/invoice_view.html` - عرض الفاتورة
- ⏳ `frontend/pages/reports.html` - التقارير
- ⏳ `frontend/pages/settings.html` - الإعدادات

### 3️⃣ ملفات إضافية

- ✅ `README_NEW.md` - دليل النظام الجديد باللغتين
- ✅ `redirect.html` - صفحة اختيار بين النظام القديم والجديد

---

## 🎯 كيفية استخدام النظام الجديد

### الخطوة 1: الوصول للنظام

افتح المتصفح وانتقل إلى:

```
http://localhost/AMZ/redirect.html
```

أو مباشرة إلى النظام الجديد:

```
http://localhost/AMZ/frontend/pages/index.html
```

### الخطوة 2: فهم طريقة العمل

**النظام الجديد يعمل كالتالي:**

1. **Frontend (HTML/JS)** يرسل طلبات إلى **Backend (PHP API)**
2. **Backend** يعالج البيانات ويتواصل مع قاعدة البيانات
3. **Backend** يرجع البيانات بصيغة JSON
4. **Frontend** يعرض البيانات للمستخدم

**مثال عملي:**

```javascript
// Frontend يطلب قائمة المنتجات
const products = await API.getProducts();

// Backend يرجع البيانات
[
  { id: 1, name: "منتج 1", price: 100 },
  { id: 2, name: "منتج 2", price: 200 },
];

// Frontend يعرض البيانات في جدول
```

---

## 📚 شرح الملفات الرئيسية

### Backend Config (`backend/config.php`)

```php
// يحتوي على:
- Database class (Singleton pattern)
- CORS headers
- دوال JSON response
- دوال تنظيف البيانات
```

### Backend Classes

```php
Product.php   // إدارة المنتجات
Invoice.php   // إدارة الفواتير
Settings.php  // إدارة الإعدادات
Reports.php   // التقارير والإحصائيات
```

### API Endpoints

```php
products.php  // GET, POST, PUT, DELETE للمنتجات
invoices.php  // GET, POST, PUT, DELETE للفواتير
settings.php  // GET, POST للإعدادات
reports.php   // GET للتقارير
```

### Frontend API Module (`api.js`)

```javascript
class API {
  static async getProducts()      // جلب المنتجات
  static async createProduct()    // إنشاء منتج
  static async getInvoices()      // جلب الفواتير
  static async createInvoice()    // إنشاء فاتورة
  // ... إلخ
}
```

### Frontend Main JS (`main.js`)

```javascript
// يحتوي على:
-showAlert() - // عرض الرسائل
  formatCurrency() - // تنسيق العملة
  formatDate() - // تنسيق التاريخ
  loadSettings(); // تحميل الإعدادات
// ... دوال مساعدة أخرى
```

---

## 🔧 ما يجب فعله الآن

### 1. اختبار النظام الحالي ✅

افتح `http://localhost/AMZ/frontend/pages/index.html` وتحقق من:

- ✅ عرض الإحصائيات
- ✅ عرض أفضل المنتجات
- ✅ عرض أحدث الفواتير
- ✅ عرض تنبيهات المخزون

### 2. إكمال صفحات Frontend المتبقية ⏳

هل تريد مني إنشاء الصفحات المتبقية؟

- صفحة المنتجات (products.html)
- صفحة الفواتير (invoices.html)
- صفحة إنشاء فاتورة (create_invoice.html)
- صفحة تعديل فاتورة (edit_invoice.html)
- صفحة عرض الفاتورة (invoice_view.html)
- صفحة التقارير (reports.html)
- صفحة الإعدادات (settings.html)

### 3. اختبار API مباشرة (اختياري)

يمكنك اختبار API مباشرة باستخدام:

- **Postman**
- **Thunder Client** (إضافة VS Code)
- **المتصفح** (للطلبات GET فقط)

مثال:

```
GET http://localhost/AMZ/backend/api/products.php
GET http://localhost/AMZ/backend/api/reports.php?type=dashboard
POST http://localhost/AMZ/backend/api/products.php
```

---

## 💡 الفرق بين النظام القديم والجديد

### النظام القديم (Monolithic):

```
index.php = HTML + PHP Logic + Database
❌ كل شيء في ملف واحد
❌ صعوبة الصيانة
❌ لا يمكن استخدامه مع تطبيقات موبايل
```

### النظام الجديد (Separated):

```
Frontend (HTML/JS) <--JSON--> Backend (PHP API) <--> Database
✅ فصل واضح بين Frontend و Backend
✅ سهل الصيانة والتطوير
✅ يمكن بناء تطبيق موبايل يستخدم نفس API
✅ كود نظيف ومحترف
```

---

## ❓ أسئلة شائعة

**س: هل النظام القديم سيتوقف عن العمل؟**
ج: لا، النظام القديم موجود ويعمل بشكل طبيعي في الملفات الأصلية (index.php, products.php, إلخ)

**س: هل يجب حذف الملفات القديمة؟**
ج: احتفظ بها كنسخة احتياطية حتى تتأكد من عمل النظام الجديد بشكل كامل

**س: كيف أختبر API؟**
ج: افتح `http://localhost/AMZ/backend/api/products.php` في المتصفح، يجب أن ترى JSON

**س: هل البيانات مشتركة؟**
ج: نعم، كلا النظامين يستخدمان نفس قاعدة البيانات

---

## 📞 الخطوات التالية

**هل تريد مني:**

1. ✅ إنشاء باقي صفحات Frontend؟
2. ✅ شرح أي جزء معين بالتفصيل؟
3. ✅ إضافة مميزات جديدة؟

**أخبرني بما تحتاجه! 🚀**
