# 🚀 خطوات سريعة - نظام الرسوم والعمولات

## ✅ تم الانتهاء من التطوير!

تم بنجاح إضافة نظام شامل لإدارة رسوم أمازون (العمولات، الشحن، التخزين) 🎉

---

## 📋 ما تم إنجازه

### 1. قاعدة البيانات ✅

- إضافة حقول الرسوم الافتراضية للمنتجات
- إضافة حقول الرسوم الفعلية للفواتير
- حساب صافي الربح تلقائياً

### 2. Backend Classes ✅

- `Product.php` - يدعم الرسوم الافتراضية
- `Invoice.php` - يحسب الرسوم تلقائياً
- `Reports.php` - يعرض صافي الربح والرسوم

### 3. API Endpoints ✅

- جميع نقاط النهاية تدعم الحقول الجديدة
- التحقق من صحة البيانات

### 4. الاختبارات ✅

- ملف `test_fees.php` للاختبار الشامل
- أمثلة واقعية

---

## 🎯 الخطوات التالية (يدوياً)

### الخطوة 1: تحديث قاعدة البيانات ⚠️

افتح phpMyAdmin:

```
http://localhost/phpmyadmin
```

1. اختر قاعدة البيانات `amz_inventory`
2. اذهب إلى تبويب "SQL"
3. افتح ملف `update_fees_system.sql`
4. انسخ المحتوى والصقه
5. اضغط "Go" أو "تنفيذ"

**أو عبر سطر الأوامر:**

```bash
cd C:\xampp\htdocs\AMZ
mysql -u root -p amz_inventory < update_fees_system.sql
```

---

### الخطوة 2: اختبار النظام 🧪

افتح في المتصفح:

```
http://localhost/AMZ/test_fees.php
```

**ما يجب أن تراه:**

- ✅ إنشاء منتج تجريبي مع رسوم
- ✅ حساب صافي الربح
- ✅ إنشاء فاتورة مع الرسوم
- ✅ عرض الإحصائيات

**إذا ظهرت أخطاء:**

- تأكد من تنفيذ ملف SQL
- تحقق من اتصال قاعدة البيانات

---

### الخطوة 3: تحديث المنتجات الموجودة 📦

يمكنك تحديث المنتجات الموجودة عبر:

#### أ) الواجهة (عند إنشاء Frontend)

- اذهب إلى صفحة المنتجات
- عدّل كل منتج وأضف الرسوم

#### ب) API مباشرة:

```bash
PUT http://localhost/AMZ/backend/api/products.php?id=1
Content-Type: application/json
```

```json
{
  "name": "اسم المنتج",
  "sku": "SKU-001",
  "purchase_price": 100.0,
  "selling_price": 200.0,
  "amazon_fee_percent": 15.0,
  "default_shipping_fee": 10.0,
  "default_storage_fee": 5.0,
  "stock_quantity": 50,
  "min_stock_level": 5
}
```

#### ج) SQL مباشر (للتحديث الجماعي):

```sql
-- مثال: تعيين رسوم افتراضية لجميع المنتجات
UPDATE products
SET
  amazon_fee_percent = 15.00,
  default_shipping_fee = 10.00,
  default_storage_fee = 5.00
WHERE id > 0;
```

---

### الخطوة 4: إنشاء أول فاتورة مع رسوم 🧾

#### عبر النظام القديم:

افتح `create_invoice.php` وأنشئ فاتورة - ستحسب الرسوم تلقائياً!

#### عبر API:

```bash
POST http://localhost/AMZ/backend/api/invoices.php
```

```json
{
  "customer_name": "اسم العميل",
  "items": [
    {
      "product_id": 1,
      "quantity": 2,
      "unit_price": 200.0,
      "purchase_price": 100.0,
      "subtotal": 400.0
    }
  ]
}
```

---

## 📊 كيفية استخدام النظام

### إضافة منتج جديد:

```json
{
  "name": "هاتف ذكي",
  "sku": "PHONE-001",
  "purchase_price": 1000.0,
  "selling_price": 1800.0,
  "amazon_fee_percent": 15.0, // عمولة أمازون 15%
  "default_shipping_fee": 25.0, // 25 ريال شحن
  "default_storage_fee": 10.0, // 10 ريال تخزين
  "stock_quantity": 50
}
```

**النتيجة:**

- الربح الإجمالي: 800 ر.س (1800 - 1000)
- عمولة أمازون: 270 ر.س (1800 × 15%)
- إجمالي الرسوم: 305 ر.س (270 + 25 + 10)
- **صافي الربح: 495 ر.س** ✨

### الرسوم تختلف حسب المنتج:

| المنتج       | العمولة | الشحن  | التخزين |
| ------------ | ------- | ------ | ------- |
| إلكترونيات   | 15%     | 25 ر.س | 10 ر.س  |
| كتب          | 15%     | 10 ر.س | 5 ر.س   |
| ملابس        | 17%     | 15 ر.س | 8 ر.س   |
| أدوات منزلية | 12%     | 20 ر.س | 7 ر.س   |

---

## 🔍 التحقق من النتائج

### لوحة التحكم:

```
http://localhost/AMZ/frontend/pages/index.html
```

ستشاهد:

- إجمالي المبيعات
- الربح الإجمالي
- **إجمالي الرسوم** (جديد)
- **صافي الربح** (جديد)

### API التقارير:

```bash
GET http://localhost/AMZ/backend/api/reports.php?type=dashboard
```

النتيجة:

```json
{
  "total_sales": 50000.0,
  "total_profit": 20000.0,
  "total_fees": 6000.0,
  "net_profit": 14000.0
}
```

---

## 📚 الملفات المهمة

| الملف                         | الوصف                               |
| ----------------------------- | ----------------------------------- |
| `update_fees_system.sql`      | تحديث قاعدة البيانات (شغّله أولاً!) |
| `test_fees.php`               | اختبار شامل للنظام                  |
| `FEES_GUIDE_AR.md`            | دليل تفصيلي 50+ صفحة                |
| `backend/classes/Product.php` | إدارة المنتجات مع الرسوم            |
| `backend/classes/Invoice.php` | حساب الرسوم تلقائياً                |
| `backend/classes/Reports.php` | التقارير مع صافي الربح              |

---

## ⚡ اختصارات سريعة

### اختبار سريع (نسخ ولصق في Terminal):

```bash
# تشغيل SQL
cd C:\xampp\htdocs\AMZ
mysql -u root amz_inventory < update_fees_system.sql

# فتح صفحة الاختبار
start http://localhost/AMZ/test_fees.php

# فتح لوحة التحكم
start http://localhost/AMZ/frontend/pages/index.html
```

---

## ❓ أسئلة شائعة

**س: هل يجب تحديد الرسوم لكل منتج؟**
ج: لا، القيم الافتراضية 0 - لكن يفضل تحديدها للدقة.

**س: هل يمكن تغيير الرسوم في الفاتورة؟**
ج: نعم! يمكن تمرير رسوم مخصصة عند إنشاء الفاتورة.

**س: ماذا عن الفواتير القديمة؟**
ج: ستظهر برسوم = 0، صافي الربح = الربح الإجمالي.

**س: كيف أحسب نسبة العمولة الصحيحة؟**
ج: راجع جدول رسوم أمازون حسب فئة المنتج:

- Electronics: 8-15%
- Books: 15%
- Clothing: 17%
- Home & Kitchen: 15%

---

## 🎉 جاهز للانطلاق!

النظام جاهز 100%. فقط:

1. ✅ شغّل ملف SQL
2. ✅ اختبر عبر `test_fees.php`
3. ✅ ابدأ الاستخدام!

**استمتع بنظام محاسبة دقيق يراعي جميع التكاليف! 🚀**

---

**آخر تحديث:** نوفمبر 2024  
**الإصدار:** 2.0.0  
**الحالة:** 🟢 Production Ready
