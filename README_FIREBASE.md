# نظام إدارة المخزون والفواتير - Amazon Store

نظام شامل لإدارة المخزون والفواتير مع دعم كامل للغة العربية.

## 🚀 المميزات

- ✅ إدارة المنتجات (إضافة، تعديل، حذف)
- ✅ إدارة الفواتير مع حساب تلقائي للأرباح
- ✅ تقارير مفصلة للمبيعات والأرباح
- ✅ تنبيهات للمنتجات منخفضة المخزون
- ✅ إعدادات قابلة للتخصيص
- ✅ واجهة عربية بالكامل مع دعم RTL
- ✅ قاعدة بيانات Firebase (لا يتطلب خادم PHP)

## 📋 المتطلبات

- حساب Firebase مجاني
- متصفح حديث (Chrome, Firefox, Edge)
- اتصال بالإنترنت

## 🔧 التثبيت والإعداد

### 1. إنشاء مشروع Firebase

1. انتقل إلى [Firebase Console](https://console.firebase.google.com)
2. أنشئ مشروع جديد
3. في قسم **Build**، اختر **Firestore Database**
4. انقر على **Create Database** واختر وضع البداية (Start mode)
5. اختر موقع الخادم الأقرب إليك

### 2. الحصول على إعدادات Firebase

1. في Firebase Console، اذهب إلى **Project Settings** (⚙️ > Project settings)
2. انزل إلى قسم **Your apps**
3. انقر على أيقونة الويب `</>`
4. سجل التطبيق واحصل على إعدادات `firebaseConfig`

### 3. تكوين المشروع

افتح ملف `firebase-config.js` واستبدل القيم بإعدادات مشروعك:

```javascript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
};
```

### 4. إعداد قواعد Firestore

في Firebase Console > Firestore Database > Rules، استخدم هذه القواعد:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

⚠️ **ملاحظة:** هذه القواعد للتطوير فقط. في الإنتاج، يجب تطبيق قواعد أمان أقوى.

### 5. إضافة البيانات الأولية (اختياري)

قم بإضافة مستند في مجموعة `settings`:

```
Collection: settings
Document ID: (auto-generated)
Fields:
  - setting_key: "store_name_ar"
    setting_value: "متجر أمازون"

  - setting_key: "currency"
    setting_value: "ر.س"
```

## 🌐 النشر على GitHub Pages

### الطريقة الأولى: من إعدادات المستودع

1. ادفع الملفات إلى GitHub
2. اذهب إلى Settings > Pages
3. اختر Branch: `main` و Folder: `/ (root)`
4. احفظ واتنظر بضع دقائق
5. سيكون الموقع متاحاً على: `https://USERNAME.github.io/REPO_NAME/`

### الطريقة الثانية: باستخدام GitHub Actions

أنشئ ملف `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
```

## 📱 الاستخدام

### الصفحة الرئيسية

- عرض الإحصائيات العامة
- روابط سريعة للصفحات المختلفة

### إدارة المنتجات

- إضافة منتجات جديدة مع السعر والكمية
- تعديل أو حذف المنتجات
- تنبيه للمنتجات منخفضة المخزون (باللون الأصفر)

### إدارة الفواتير

- إنشاء فواتير جديدة
- إضافة منتجات متعددة لكل فاتورة
- حساب تلقائي للضرائب والخصومات
- حفظ معلومات العميل

### التقارير

- تقارير المبيعات حسب التاريخ
- إجمالي الإيرادات والأرباح
- تحليل المنتجات الأكثر مبيعاً

### الإعدادات

- تخصيص اسم المتجر
- تغيير العملة
- تعديل معلومات الاتصال

## 🔒 الأمان

⚠️ **هام للإنتاج:**

1. **لا تضع ملف `config.php` في GitHub** إذا كان يحتوي على معلومات حساسة
2. قم بتفعيل **Firebase Authentication** لحماية البيانات
3. استخدم **Firestore Security Rules** المناسبة:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
```

## 📝 هيكل المشروع

```
AMZ/
├── index.html              # الصفحة الرئيسية
├── products.html           # إدارة المنتجات
├── invoices.html           # قائمة الفواتير
├── create_invoice.html     # إنشاء فاتورة جديدة
├── reports.html            # التقارير
├── settings.html           # الإعدادات
├── firebase-config.js      # إعدادات Firebase
├── js/
│   └── firebase-api.js     # طبقة API
├── .gitignore              # ملفات مستثناة من Git
└── README.md               # هذا الملف
```

## 🐛 حل المشاكل

### المشكلة: "Firebase is not configured"

**الحل:** تأكد من تعديل ملف `firebase-config.js` بإعدادات مشروعك

### المشكلة: "Permission denied"

**الحل:** تحقق من قواعد Firestore في Firebase Console

### المشكلة: النصوص العربية لا تظهر بشكل صحيح

**الحل:** تأكد من أن الصفحة تستخدم UTF-8 encoding

### المشكلة: البيانات لا تظهر

**الحل:** افتح Console في المتصفح (F12) وتحقق من رسائل الخطأ

## 📞 الدعم

إذا واجهت أي مشكلة، يمكنك:

- فتح Issue على GitHub
- مراجعة [وثائق Firebase](https://firebase.google.com/docs)
- مراجعة [وثائق GitHub Pages](https://pages.github.com)

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 🙏 شكر خاص

- Bootstrap 5 للواجهة
- Firebase لقاعدة البيانات
- Bootstrap Icons للأيقونات

---

**ملاحظة:** هذا النظام مناسب للمشاريع الصغيرة والمتوسطة. للمشاريع الكبيرة، يُنصح باستخدام خادم مخصص وقاعدة بيانات MySQL.
