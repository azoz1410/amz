# تحديثات النظام - الإصدار 2.0

## المميزات الجديدة

### 1. ✨ صفحة إعدادات المتجر

تم إضافة صفحة إعدادات شاملة تتيح لك:

- **تغيير اسم المتجر** (عربي وإنجليزي)
- تعديل معلومات الاتصال (هاتف، بريد إلكتروني، عنوان)
- اختيار العملة من بين 5 عملات
- تحديد نسبة الضريبة الافتراضية
- تخصيص نص ختام الفاتورة

**للوصول:** الإعدادات من القائمة الرئيسية

### 2. ✏️ تعديل الفواتير

يمكنك الآن تعديل أي فاتورة موجودة:

- تعديل معلومات العميل
- إضافة أو حذف منتجات
- تغيير الكميات
- تعديل الخصومات والضرائب
- تغيير حالة الفاتورة (مدفوعة/معلقة/ملغاة)

**النظام يقوم تلقائياً بـ:**

- إرجاع المنتجات القديمة للمخزون
- خصم المنتجات الجديدة من المخزون
- إعادة حساب الأرباح

### 3. 🗑️ حذف الفواتير

إمكانية حذف أي فاتورة مع:

- إرجاع تلقائي لجميع المنتجات للمخزون
- تسجيل الحركة في سجل المخزون
- تأكيد قبل الحذف لتجنب الأخطاء

### 4. 🏪 تخصيص اسم المتجر

- يظهر اسم المتجر المخصص في جميع الصفحات
- يظهر في الفواتير المطبوعة
- دعم اللغتين العربية والإنجليزية

## خطوات تطبيق التحديثات

### الخطوة 1: تحديث قاعدة البيانات

افتح phpMyAdmin وقم بتنفيذ ملف `update_database.sql`:

```sql
-- أو انسخ والصق هذا الكود:

CREATE TABLE IF NOT EXISTS settings (
    id INT AUTO_INCREMENT PRIMARY KEY,
    setting_key VARCHAR(100) UNIQUE NOT NULL,
    setting_value TEXT,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

INSERT INTO settings (setting_key, setting_value) VALUES
('store_name', 'Amazon Store'),
('store_name_ar', 'متجر أمازون'),
('store_phone', '+1234567890'),
('store_email', 'info@amazon-store.com'),
('store_address', ''),
('store_website', ''),
('store_logo', ''),
('tax_rate', '0'),
('currency', '$'),
('invoice_footer', 'شكراً لتعاملكم معنا')
ON DUPLICATE KEY UPDATE setting_value=VALUES(setting_value);
```

### الخطوة 2: الملفات الجديدة

تأكد من وجود هذه الملفات:

- ✅ `settings.php` - صفحة الإعدادات
- ✅ `edit_invoice.php` - صفحة تعديل الفواتير
- ✅ `update_database.sql` - تحديثات قاعدة البيانات

### الخطوة 3: تحديث config.php

تم إضافة دوال جديدة:

- `get_setting()` - للحصول على إعداد معين
- `save_setting()` - لحفظ إعداد
- `get_all_settings()` - للحصول على جميع الإعدادات

## استخدام المميزات الجديدة

### تخصيص المتجر

1. اذهب إلى: **الإعدادات**
2. أدخل اسم متجرك (مثلاً: "متجر الإلكترونيات")
3. أدخل معلومات الاتصال الخاصة بك
4. اختر العملة المناسبة
5. اضغط "حفظ الإعدادات"

✨ **سيظهر اسم متجرك في:**

- القائمة الرئيسية
- جميع الفواتير المطبوعة
- رأس وتذييل الفواتير

### تعديل فاتورة

1. اذهب إلى: **الفواتير**
2. اضغط على أيقونة القلم (✏️) بجانب الفاتورة
3. عدّل ما تريد
4. اضغط "تحديث الفاتورة"

⚠️ **ملاحظة:** عند تعديل الفاتورة، يتم:

- إرجاع المنتجات القديمة للمخزون
- خصم المنتجات الجديدة

### حذف فاتورة

1. اذهب إلى: **الفواتير**
2. اضغط على أيقونة السلة (🗑️) بجانب الفاتورة
3. أكد الحذف

✅ **يتم تلقائياً:**

- إرجاع جميع المنتجات للمخزون
- تسجيل الحركة في سجل المخزون

## العملات المدعومة

- 💵 دولار أمريكي ($)
- 💶 يورو (€)
- 💷 جنيه إسترليني (£)
- 💰 ريال سعودي (﷼)
- 💸 درهم إماراتي (د.إ)

## التغييرات التقنية

### قاعدة البيانات

- جدول جديد: `settings` لتخزين إعدادات المتجر
- دعم Unicode الكامل (utf8mb4)

### الملفات المحدثة

- ✅ `config.php` - إضافة دوال الإعدادات
- ✅ `index.php` - عرض اسم المتجر المخصص
- ✅ `invoice_view.php` - عرض بيانات المتجر المخصصة
- ✅ `invoices.php` - إضافة أزرار التعديل والحذف
- ✅ جميع الصفحات - إضافة رابط الإعدادات في القائمة

## الأمان

- ✅ تنظيف جميع المدخلات
- ✅ منع SQL Injection
- ✅ منع XSS
- ✅ تأكيد قبل الحذف
- ✅ تسجيل جميع حركات المخزون

## الدعم الفني

إذا واجهت أي مشكلة:

1. تأكد من تنفيذ `update_database.sql`
2. تأكد من صلاحيات الملفات
3. راجع سجل أخطاء PHP

---

**تاريخ التحديث:** نوفمبر 2025  
**الإصدار:** 2.0  
**التحديثات الرئيسية:** إعدادات المتجر، تعديل الفواتير، حذف الفواتير
